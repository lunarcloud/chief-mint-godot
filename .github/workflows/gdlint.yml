name: GDScript Lint

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  gdlint:
    runs-on: ubuntu-latest
    name: Lint GDScript files
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        
    - name: Install gdtoolkit
      run: |
        python -m pip install --upgrade pip
        pip install "gdtoolkit>=3.0,<4.0"
        
    - name: Run gdlint
      run: |
        # Find all .gd files and run gdlint on them
        find . -name "*.gd" -type f ! -path "./.godot/*" ! -path "./.git/*" -print0 | xargs -0 gdlint || true
        echo "GDScript linting completed"
